{
  "name": "dark-fantasy",
  "version": "0.3.0",
  "private": true,
  "description": "Dark Fantasy — multiplayer arena survival (React + TS + FastAPI)",
  "scripts": {
    "dev": "pnpm --filter @dark-fantasy/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "e2e": "pnpm --filter @dark-fantasy/game e2e && pnpm --filter @dark-fantasy/game debug-e2e && pnpm --filter @dark-fantasy/web e2e",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.13"
  }
}